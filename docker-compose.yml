version: '3.8'

services:
  hackathon-dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    container_name: hackathon-dashboard
    restart: unless-stopped
